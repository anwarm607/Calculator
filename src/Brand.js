import React from "react";
import "./Brand.css";

const Brand = props => {
  return (
    <section className="brand">
      <i className="fab fa-react"></i>
    </section>
  );
};

export default Brand;